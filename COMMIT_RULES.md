# Git 提交规范

- 提交标题必须使用中文，准确概括主要改动；不得只写 `update`、`fix`、`chore` 等笼统说明。
- 改动较多时，提交正文按以下顺序列出：
  1. 完成内容；
  2. 影响范围（模块、接口、页面或文档）；
  3. 验证结果、遗留风险或下一步。
- 模块名、命令、技术术语可以保留英文；提交说明主体必须为中文。
- 提交前检查 diff，禁止混入密钥、token、构建产物、个人环境配置和无关修改。
